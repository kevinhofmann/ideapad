/* const titleElement = document.querySelector("#idea-title")
const descriptionElement = document.querySelector("#idea-description")
const reasonElement = document.querySelector("#idea-reason")

const ideaID = location.hash.substring(1)


const saved = getSavedIdeas()
console.log(saved)
const getIdeaByID = function(id) {
    return saved.find(function(idea) {
        idea.id === id
    })
}

const allIdeass = getIdeaByID(ideaID)


console.log(allIdeass)
titleElement.value = saved.title */